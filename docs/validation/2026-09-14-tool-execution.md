# 步骤执行能力：完整集成验证

日期：2026-09-14（Asia/Shanghai）。分支 `codex/step-execution`，工作树 `.worktrees/step-execution`；接续[输出要求](2026-09-14-output-policy.md)及[项目默认与指导目录](2026-09-14-execution-settings.md)。仅本地实现与临时演示项目验证，未合入 main、推送、发布或替换已安装 Mira。

## 产品命题

输出风格、单项 Skill、工具选择是独立设置。Skill 的冻结文本进入普通模型请求，必需工具依赖不自动授权。连接发现、项目启用、本步绑定互相独立。MCP/已审阅脚本用于生成前或模型按需，后检查只由检查器执行；临时 Python 先审阅实际代码。所有执行仍使用同一 Run、冻结来源、目标基线、CAS/Candidate 和有限预算。完整规则见[执行契约](../specs/tool-execution.md)。

## 实现正确性

- `node-mcp-client.test.js` 通过真实本地 HTTP/SSE 与 stdio 子进程验证握手、分页、目录变化、认证/重定向失败、响应上限、取消、超时、session 清理与未知结果不重放。发现不调用业务工具；同名工具由稳定连接身份区分。
- `openai-compatible-adapter.test.js` 通过真实本地 HTTP 服务完成 tool_call_id 往返，覆盖未知工具/坏 JSON/重复 ID、整批预检、轮数/次数、超限、超时、取消及确定失败与未知结果的区别。Kimi 所需 reasoning 协议字段只留当次内存，不存入 Run；这不是线上 Kimi 模型实测。
- `tool-run-integration.test.js` 使用真实临时文件系统与应用路由验证 Run 前拒绝缺能力/Skill 依赖、冻结工具配置、逐次审批、旧批准失效、方法待绑定、人工 Head 变动进入 Candidate、附件省略与秘密排除。`tool-security.test.js` 覆盖 schema 复杂度、参数边界、一次审批、冻结环境、后检查预算、写入失败停止等。
- 关闭宿主先拒绝仍在准备的 Run；取消后模型即使先返回，也保留待中断身份到终态持久化。能力 HTTP 断开会透传取消，主工作区写锁在请求排空后释放。指导依赖拒绝非数组输入并复制调用方数组，避免保存后被别名修改。
- 参数结构保留合法参数名（如 token 的类型声明），同时拒绝 default/examples/const/enum 携带真实认证数据。方法去掉机器绑定、参数、URL 和批准；Board/Checkpoint 副本要求重新绑定外部能力。配置、凭据、脚本目录不进入可移植包，历史附件明确标记省略。

独立切片使用各自 worktree 和提交，主 Agent 按依赖集成，再由另一 Agent 对最终交叉路径复查。复查发现并修复了关闭竞态、HTTP 取消遗漏、凭据跨目的地、schema 示例秘密、未预留后检查预算等问题；回归先复现再修正。

最终工程门禁：`pnpm test` **177 文件 / 1799 项通过**；`pnpm exec tsc --noEmit`、`pnpm build`、`pnpm build:bridge` 与 `git diff --check` 通过。Node 26 的既有 experimental localStorage 警告仍出现于测试进程，不是浏览器警告。主入口 388.77 kB、步骤详情按需块 131.75 kB、能力管理按需块 43.24 kB；构建没有超过 500 kB 的前端 chunk 警告。

## 真实 Python 隔离

主 Agent 在当前集成代码执行 `node scripts/verify-python-isolation.mjs`，返回 passed=true。运行时 Docker Linux / Python 3.13.15，镜像 `sha256:9d2e5553305c7c7b0097999bb17187c69b921ccd6bc9d40e4bb5ebe652c00285`。

真实检查包括：宿主哨兵文件不可见、无网络、根目录不可写、冻结 stdin 范围、文本与附件、字节限制、路径拒绝、超时清理、主动取消清理和 Host 崩溃后的容器内时限。验证清理只处理本次随机 owner 的容器，未触及用户容器。异常 Host 退出后可能留下已停止容器，不声称防御恶意 daemon 或内核漏洞。

显式环境准备与固定依赖另在临时项目验证；`packaging==24.2` 的固定镜像为 `sha256:883c0f968903d200e666a45df78319383630e6e85405c7998e5452171776d89e`。测试期间下载镜像/依赖，不发生模型费用，普通 Run 不安装。

## UX 可发现性与浏览器

真实浏览器使用临时 `步骤能力验收 · 演示材料` 项目，1280×720、1024×768 和 390×844。未打开或修改真实用户 Board/Run。

- 系统设置进入能力管理；默认简洁规则可见；MCP 地址明确测试后只列目录，项目手动启用后才可在本步选择。
- MCP 本地内存工具先展示完整参数与来源。拒绝后 Run 失败、零业务调用、无成果覆盖；新 Run 重新审批，批准后恰一次真实调用并展示成功证据，普通成果写回。
- 390px 审阅与浅色能力表单均无页面横向溢出。1024px 代码表单保留独立滚动，正文不撑宽窗口；新能力表单可见按钮至少 44px 高。
- Python 环境准备、实际镜像核对、粘贴完整代码、隔离测试、CSV 预览、保存版本 1 与项目启用成功。测试来源数量为 0，预览返回 CSV `value / 42`，下载只在展开后出现。修改测试参数后“保存为新版本”禁用，离页保护聚焦“继续编辑”，放弃后正确返回画布。
- 工具选择、搜索/来源筛选、前后阶段、列表上移、冻结参数保存与临时代码开关可用。不支持的方式禁选。浅色/深色、长参数、错误状态与焦点外观已目视检查。

最终浏览器确认“仅重跑这一步”使用新保存配置，上游未生成目标保持原状；旧成果继续可见，成功后追加 v2/v3。真实调用顺序为数字运算 42 → 模型提出 Python → 审阅完整代码、精确参数和冻结镜像 → 批准后 Docker 返回一份冻结来源及 CSV → 正文后检查。刻意设置 5 字符上限，46 字符成果仍完整采用，检查单独显示“未通过”，没有静默截断或重跑。新入口的双击锁、未保存保护、活动 Run/Candidate/缺依赖禁用与停止另有真实 React DOM 回归。

390px 长代码在 357px 宽容器内部横向滚动，页面 scrollWidth=390，审批按钮均 44px 高；1024/1280 页面同样无横向溢出。最终展开 CSV 显示 `value / 42` 和明确下载链接。浏览器 console 捕获无 warning/error。

## 桌面与其他平台

本机 macOS arm64 定向桌面测试 **9 文件 / 91 项通过**；最终 `pnpm desktop:make:arm64`、`pnpm desktop:smoke:packed:arm64`、`node scripts/packed-desktop-smoke.mjs --arch=arm64 --restore` 全部通过。使用独立临时 userData/workspace，普通启动含 PDF 原件读取，恢复模式核对备份/检查点；不以这些 smoke 代替可见客户端验收。

未签名内部测试产物：`out/desktop/make/Mira-0.1.0-beta.2-arm64.dmg`，SHA-256 `4c74af9ef792a0368e340d1d867240c594fa1f21ebd5cc189ffc4c5765ef3b09`；同目录构建同时生成 arm64 ZIP。没有替换 `/Applications` 中的安装版。

本轮原生 Mac 会话锁屏，计算机工具报告无法自动解锁；未尝试绕过。因此没有本批原生窗口、目录选择器、焦点或可见桌面客户端验收证据。上一批窗口证据不替代本批；Windows/Intel 原生、真实触屏软键盘、安全区、签名、公证与发布同样未验证。

## 产物质量与实用边界

这批证明设置可达、调用真实、范围/审批/写回受控。浏览器使用明确标注的演示材料和本地协议服务，不能据此声称模型更准确、写作风格必然服从或降低人工修改成本。EXEC-10 同材料/模型/任务的线上质量对照仍没有新增证据，未执行付费 Kimi 调用。

能力兼容性受支持的 JSON Schema 子集、文本/结构化 MCP 返回、Docker Linux 和镜像依赖约束；不兼容时明确失败。操作入口及示例见[使用步骤能力](../operations/tool-capabilities.md)。

## 08:01–08:24 补充复验：已解锁的 macOS 原生窗口

用户要求“现在验证一下”后，在实现提交 `0b85c49` 重新验证。上文锁屏事实保留；本节补充本次新证据，不回写上一轮结论。结论：主要执行路径通过，发现一项可复现的“重试”入口缺陷，本批不能标为全部 UX 验收通过。本轮只验证并登记发现，未修改生产代码。

### 工程与隔离

- 重新运行 `pnpm test`：177 文件、1799 项通过；`pnpm exec tsc --noEmit`、`pnpm build`、`pnpm build:bridge`、`git diff --check` 均通过。
- 再次执行 `node scripts/verify-python-isolation.mjs`：passed=true，仍为上述 Docker Linux / Python 3.13.15 镜像；八类真实隔离、范围、附件、上限、路径和清理检查通过。
- 同一 arm64 打包产物再次通过普通和 `--restore` 两种 packed smoke。本次无生产代码变更，未重复 make；DMG SHA-256 与上文一致。

### 原生交互与实际写回

从本工作树启动打包 `.app`，使用独立临时 userData 与空项目 `/tmp/mira-native-verify.V1lcT1/project`。未替换安装版、未写入真实用户工作区。原生目录选择器通过“前往文件夹”与“使用此文件夹”进入项目。测试窗口约 1229×768，实际检查浅色/深色、表单输入焦点、原生下拉菜单、侧栏滚动、审阅与 CSV 预览；这些操作没有被遮挡或横向溢出阻止。

所有卡片和步骤均在原生 UI 手动创建、配置与运行，材料明确标注为演示。模型使用本地 HTTP 协议服务 `local-native-validation`，MCP 使用本地内存工具；不是线上模型质量实测。

1. 模型设置测试连接成功。默认输出为“卡片简洁”；选择“证据对照”指导并填写完成标准后，风格与指导独立保留。未填写指导标准时保存不可用，选择指导不自动增加工具。
2. 配置“数字运算 · 模型按需”和“正文检查 · 生成后”。无效参数可保存计划，但运行前被阻止且没有创建 Run；补齐计算参数并将误填的 `maxChars` 改为 schema 声明的 `maxCharacters` 后正常运行。
3. 实际工具返回 42；44 字符的全文追加为目标 v1。5 字符上限检查为未通过，完整正文仍保留。运行详情同时显示冻结指导/风格版本、真实计算参数与结果、正文检查 `passed:false`，没有自动截断或重跑。
4. MCP 经发现、项目启用、本步绑定三个显式操作后，进入逐次审阅。拒绝时服务端业务调用数为 0，Run 为 `TOOL_REJECTED`，旧 Head 不变。通过“仅重跑这一步”另开运行并批准后，业务调用恰好一次，继续模型与检查器并追加目标 v2。
5. 原生能力管理准备 Python 环境成功，镜像身份与隔离测试一致。脚本实际测试返回“测试来源数量：0”，CSV 展开可见 `value / 42`。登记追加版本 1，默认未启用；明确项目启用后状态变为已启用。测试与登记不创建 Card/Run。

结束后核对临时项目持久化证据：仅三次 Run（成功、拒绝失败、成功），两次成功均保留检查未通过的完整结果；脚本 1 个、已启用工具 2 个。测试应用正常退出并停止本地协议服务，保留临时演示项目供复核。

### 发现：已有成果时卡片“重试”没有发起新运行（P2，待修复）

复现：先生成成功，再添加需要审阅的工具，通过“仅重跑这一步”运行并拒绝；目标卡保留旧成果并出现“重试”。点击卡片“重试”却提示“到……已经是最新”，未创建 Run。详情中的“仅重跑这一步”可正确发起新审批并完成。

代码证据：`src/v2/ContentCard.tsx:94` 对 failed 状态统一展示“重试”，调用 `runToTransformation`；`src/v2/runSlice.ts` 对 `transformationExecutionDecision` 的 `current` 分支直接跳过。旧成功成果及来源未变化时，这个入口与用户明确的重试意图不一致。此外它未区分本次拒绝错误的 `retryable:false`。需要另行修正入口行为与提示，并增加这一实际场景的回归，不能只因现有 1799 项通过而忽略。

### 日志与尚未覆盖范围

首次目录选择期间，Electron 日志曾出现网络子进程启动连接超时、network service 重启，随后输出 ready，后续本地模型、MCP 与 Python 调用正常；另有 `fs.Stats` 弃用和 macOS 输入法 mach-port 诊断。本次不声称原生日志零 warning/error，也未确定首次子进程诊断的根因。后续普通/恢复 packed smoke 均成功。

本次补齐 macOS arm64 的可见窗口及核心执行交互证据；发现的重试问题仍未关闭。390px 仍引用上一轮真实浏览器证据，未以原生窗口代替窄屏。Windows/Intel 原生、真实触屏软键盘与线上同材料/模型质量对照仍未验证；临时 Python 的完整模型代码审阅沿用上一轮浏览器证据，本次原生验证为已登记脚本的测试/预览流程。

## 08:25–08:36 修复复验：卡片失败入口

本节关闭上节 P2“重试”缺陷；上节保留修复前的真实发现。产品结果仍是用户明确发起单步 Run，不扩大 stale、不重跑上游、不沿用旧工具批准。

- `ContentCard` 对明确 `retryable:true` 的失败调用既有 `rerunTransformation`，复用草稿保护与 `runExclusiveAction`，提交中防重复点击，其他 queued/running Run 期间禁用。非可重试错误显示“查看原因”，只打开该次运行记录；核对后仍可从生成步骤明确重新运行。交互权威说明已补入体验设计 §2.4。
- 先添加四个组件回归，运行出现三个预期失败（错误调用路径、不可重试入口、其他活动 Run 禁用）；最小实现后全部通过。另一个新增测试锁定既有草稿保护。测试使用真实 Card 组件与投影，运行调度边界使用替身；实际新 Run 与 Version 由以下真实宿主验证补齐。
- 定向组件、Store 与 Bridge 工具 Run 集成测试 32 项通过；最终全仓 **177 文件 / 1803 项通过**。类型检查、前端构建、Bridge 构建及 diff 空白检查通过；桌面定向 9 文件 / 91 项通过。测试进程保留既有 Node experimental localStorage 警告。
- 使用临时演示项目和本地 HTTP 模型协议服务，在 1280×800 浏览器、390×844 浏览器与本次重新构建的 macOS arm64 `.app` 中分别验证“先成功、再限流失败、点击卡片重试”。每个场景模型计数从 2 增为 3，持久化恰好三次 Run，目标从 v1 追加为 v2。没有出现“已经是最新”的跳过提示。
- 浏览器不可重试场景点击“查看原因”打开准确失败记录，模型计数仍为 2，持久化仍为两次 Run、一个目标 Version。桌面鼠标点击与窄屏 Enter 激活均正常，浏览器 console 无 warning/error，页面宽度与 scrollWidth 均为 390。窄屏 Fit View 会缩小画布内控件，本次不把键盘激活等同于真实触屏命中尺寸验收。
- 原生浅色窗口可见 v2 和第 3 次请求正文；应用日志只有既有 `fs.Stats` 弃用警告及 ready，本次未复现前述网络子进程重启。模型服务、浏览器页、原生测试进程均已停止，原安装版进程保持运行，临时演示项目保留供复核。
- 重新完成 arm64 make、普通与恢复两种 packed smoke。新 DMG SHA-256 为 `95cb8eae64524e002abf20d12e490a5b657d0baa4ec4f96a98395c052860cf78`；上文旧 hash 对应修复前产物。未替换安装版、未合入 main 或发布。

此次验证证明重试意图、错误入口、单次调度和版本写回正确；不是线上模型质量证据。其他平台、真实触屏和线上质量边界保持不变。
