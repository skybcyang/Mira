# 顶栏画板管理与平铺工具栏验证

日期：2026-09-08。范围：工作台入口和字标细化，沿用现有功能与领域契约。

## 交付与体验

- 管理画板放在顶栏字标旁；中央搜索与右侧撤销、重做、设置保持可达。
- 悬浮工具栏平铺新建、多选、画布、灵感池、方法、文件、搭计划、画布版本，移除更多和重复搜索。
- 字标使用带句点的 `mira.`、Georgia 衬线斜体；这是按截图特征匹配，未确认参考图的原始字体文件。
- 复用原动作与草稿保护，删除不再需要的更多菜单状态和键盘导航；没有新增依赖。

## 实际验证

- 调整导航测试后先观察到搭计划未直达、顶栏缺少管理画板两项失败；实现后通过。
- 浏览器使用隔离测试 workspace `/tmp/mira-workbench-FN1WVE`，预览端口 56311；本轮仅打开、关闭面板，不写画板数据。
- 1280×900：搜索中心与视口中心均为 640px；完整悬浮工具栏无页面横向溢出。管理画板、搭计划、画布版本可直接打开。
- 390×844：八个工具各宽 44.5px，同排可见；管理画板按钮宽 44px，页面 scrollWidth 为 390px。字标为 24px 衬线斜体。
- 窄屏计划任务打开后工具栏隐藏；关闭画板管理后焦点回到顶栏管理按钮。设置与搜索 Escape 关闭后分别返回设置和搜索入口。
- 浏览器 warning/error 日志为空。已恢复临时视口尺寸。
- `pnpm test`：133 文件、1479 测试通过（16:15:57 开始，6.42 秒）；类型检查、前端构建、Bridge 构建、`git diff --check` 通过。同步了按旧菜单 DOM 匹配的测试与视觉指纹。

## 边界

本轮验证深色浏览器外观的桌面及 390px 布局，未重复六套外观矩阵、真实触屏软键盘或 Electron 打包验收。Board、CardVersion、Run、Candidate 和持久化行为无改动。
