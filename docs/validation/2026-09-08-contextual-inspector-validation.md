# 悬浮详情与卡片跟随验证

日期：2026-09-08。基线：`37f505e`；独立分支：`codex/contextual-inspector`。

## 产品结果与可发现性

- 保留当前外观 token，详情改为距顶栏、右侧和底部各 12px 的悬浮面板。名称在顶部独立呈现，卡片与详情名称旁直接改名；移除单项更多菜单。
- 正文默认阅读，单一编辑/预览入口原位切换；标签紧凑显示，文件绑定与出处折叠到卡片信息。只展示当前可达的内容、版本、生成步骤和运行记录。
- 已打开的内容/版本详情跟随明确单选；关闭状态、多选、框选、来源选择和来源辅助阅读不会自动跳转。关系与运行任务保持原上下文。
- 未保存名称、正文与标签可以保存后继续、放弃或继续编辑。逐项保存失败即停止导航；已成功字段不回滚，失败和未尝试的草稿保留。

## 实现正确性

- 选择跟随与逐项保存为纯策略，编辑字段注册现有 API 保存回调，不引入新的领域写入协议、依赖或数据迁移。
- 基线 1495 项测试通过后，先验证跟随、失败停止、忙碌拒绝与上下文改变的失败测试，再实现。重复改名命令的回归先失败后修复；重复命令保留异步面板 revision。
- `pnpm test`：135 文件、1502 测试通过，19:47:00 开始，5.31 秒；包含现有 Bridge 存储和 HTTP 集成。
- `pnpm exec tsc --noEmit`、`pnpm build`、`pnpm build:bridge`、`git diff --check` 通过。
- 最终 diff 单 Agent 自审：检查选择入口、草稿保存顺序与失败路径、相同卡片重复命令、默认阅读与新建编辑、延迟详情模块边界和响应式覆盖。未改写领域规则。

## 真实浏览器与产物质量

预览：`http://127.0.0.1:56313/graphmind/`。只使用独立临时 workspace `/tmp/mira-inspector-pPYrXj`，没有写入用户真实画板或替换已安装 app。

- 1280×900：普通面板 380px；展开后左边界与剩余 Canvas 右边界均为 480px，没有重叠。独立名称与 Markdown 正文完整显示，标签和卡片信息折叠可展开。
- 直接改名聚焦名称输入；取消后聚焦改名按钮；再次点击同一卡片的改名命令可重新进入。新建直接聚焦正文，保存后返回阅读。
- 单选另一卡同步名称和正文；版本视图切卡进入新卡内容页；关闭详情后单选保持关闭。多选模式下单选另一卡不跳转详情。
- 同时修改名称、正文与尚未点击添加的标签，保存并切换成功；通过临时 HTTP 服务核对名称、标签和一个新增正文版本全部持久化。
- 继续编辑保留草稿并恢复 textarea 焦点。通过临时 HTTP 服务模拟另一窗口写入 Head，保存并切换收到预期 409，原详情与草稿保留；继续编辑、放弃并切换均可执行。
- 新建转化只铺结构，未运行模型；详情展示内容、生成步骤与版本，未出现无效运行页签。添加来源模式点击卡片正文只更新待选列表，取消返回原步骤。
- 390×844：详情全屏，页面 scrollWidth 390px；改名输入与按钮均至少 44px，正文底部三按钮均 44px、底边 827px；离开确认三选项完整可达，保存并继续后关闭详情。
- 1024×800：380px 模态详情距顶部 70px、右边与底部各 12px，页面 scrollWidth 1024px。
- 检查浅色和深色的阅读/编辑布局；最终构建正常流程的浏览器 warning/error 日志为空。故意制造的保存冲突单独按预期错误验证。

## 边界

本轮没有重新打包 macOS app、运行模型或验证真实触屏软键盘；没有做六套外观全矩阵。浏览器验收不等同于原生桌面发行验收。临时视口覆盖在交付前恢复。

## 后续修正：转换详情切回内容卡（2026-09-08）

用户反馈转换卡后单选内容卡不更新右栏。真实浏览器复现：画布已选择“行动清单”，右侧仍为“转化设置”。根因是 `followCardSelection` 仅允许 content/versions，上文最初的“关系保持原上下文”行为被本次反馈修正。

- 将 relation 纳入明确单选跟随，继续排除多选、框选、来源点选和运行视图；转换设置草稿接入统一离开确认，避免新增自动切换丢失输入。
- 两项回归先失败后通过：转换详情单选导航、转换表单草稿接入；不改变领域、存储或运行语义。
- `pnpm test` 135 文件、1504 测试通过（20:11:41，7.37 秒）；`pnpm exec tsc --noEmit`、`pnpm build`、`pnpm build:bridge` 和 `git diff --check` 通过。
- 1280×900：转换详情 → 内容卡立即变为该卡内容；编辑转换名称后切卡提示，继续编辑保留草稿与焦点，放弃并切换后进入目标内容卡。
- 390×844：编辑生成目标后关闭提示完整可达，两个按钮均 44px，页面无横向溢出；继续编辑保留输入，取消编辑后内容页签可用。浏览器 warning/error 为空，最终恢复视口。
- 原预览地址 56313 已更新。本次浏览器只修改并放弃临时草稿，未保存测试转换设置、未操作真实 workspace 或打包 Mac app。
