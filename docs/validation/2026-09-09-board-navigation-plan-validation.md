# 画板导航与方法计划入口验证

- 日期：2026-09-09
- 分支：`codex/board-navigation`，基线 `adbeb61`
- 范围：用户确认的统一画板菜单、手动关闭、合并方法与计划入口及计划编辑表单。
- 执行：单 worktree 完成，按本机导航、只读 activity API、UI 与计划映射顺序实现；未改变 Board 生命周期、Run 或方法应用的领域写入契约。

## 自动化

- `pnpm test`：138 文件、1519 项通过，含 Node Host 真实 HTTP 与 Workflow service/store 集成测试。
- `pnpm exec tsc --noEmit`、`pnpm build`、`pnpm build:bridge` 与 `git diff --check` 通过。
- 新增关闭失败保留、活动 Run/Candidate 阻止关闭、状态读取失败拒绝关闭、关闭最后一项与刷新空目录、迟到关闭响应失效、常用/已打开映射、步骤名称与指令分离测试。
- activity 的纯计数、路由和严格存储读取失败测试，以及真实 Node Host `/boards/activity` HTTP 验证通过。
- 先验证新增行为测试失败，再实现；旧入口和原生 select 的源代码断言、CSS 基线按确认后的设计更新。
- Node 26 测试进程存在既有 localStorage ExperimentalWarning；浏览器控制台未发现 warning/error。

## 真实浏览器

服务使用临时 workspace `/tmp/mira-navigation-bzwIEw` 与独立端口 56314；没有启动第二个写入用户 workspace 的实例。浏览器读取生产构建，验证 1280×900 与 390×844。

1. 搜索全部未归档画板，可打开不在默认列表中的画板；方向键/回车可选择，搜索获得初始焦点。
2. 固定常用后关闭当前画板，切到其他已打开画板；常用项保留且可重新打开。关闭最后一项出现“未打开画板”，重开页面保持空态，没有自动新建。
3. 窄屏菜单内新建“导航验收示例”成功并切入；菜单、底部工具栏和全屏计划表单无横向溢出，关键按钮未遮挡。
4. 方法与计划为单一导航入口；空方法库仍可从零搭计划，命令搜索保留直接计划入口。
5. 已填计划关闭时出现离开确认；继续编辑保留输入并把焦点送回名称字段，放弃后退出。最终成果同步到最后一步，新增步骤保留之前的处理要求。
6. 实际填写双步骤计划，预览时仍需明确“使用已选”绑定起始材料，确认后创建 2 个普通步骤及 2 张空成果 Card。通过 HTTP 读取验证名称分别为“用户问题清单 / 产品验证方案”，两段 instruction 独立保存，没有 lastRunId，activity 为空；界面明确“尚未生成”。
7. 桌面深色与窄屏深色截图检查通过，浅色复查使用同一主题 token。异步画板切换曾使焦点落到 body，已补充完成后回到画板触发器，且不抢走用户已移动到其他控件的焦点。

## 边界

- 已打开与常用是本机 UI 偏好，不进入 Board、备份或 CanvasHistory；画板关闭不等于归档或删除。
- 菜单打开期间每 5 秒刷新目录和任务计数，关闭前再查；失败按未知处理。当前画板的已加载任务也参与即时标记和关闭保护。
- 浏览器任务状态限制由自动化验证；未调用真实模型。窄屏验证不替代真实触屏软键盘验收。
- 本批未重新打包、替换 macOS 安装或推送远端。
