# 卡片颜色与显式分组验证

后续合并记录：2026-09-06 已快进合并到 `main` 的 `29c8ca0`，并在主工作树重跑全部门禁通过。
下方分支、worktree 与隔离预览路径记录验收当时状态；开发 worktree 后续可清理，不影响
已合并代码或保留在隔离 workspace 的截图与测试数据。

- 日期：2026-09-06
- 分支：`codex/card-grouping`
- 基线：`e8c5b7c`，独立 worktree `.worktrees/card-grouping`。
- 状态：实现、定向测试、全量门禁及浏览器验收通过；仅独立分支交付。

## 产品与范围

权威约束见产品定义 5.2.1 与核心规格 4.4.2。颜色是独立 Card 元数据，分组是单层显式成员集合而非内容或执行对象。移动始终不改变归属，解组不删 Card；不实现嵌套、折叠、拖入拖出自动归组、自动聚类或自由区域框。

主工作树中的推导链折叠文档草稿未带入、未改写。没有操作真实 Board/Run 数据，没有推送远端或合并 main。两个实现 worktree 从已提交规格基线 4965c6f 开始，各自独占 frontend 或 Bridge 文件。

## 自动化

基线 `pnpm test`：117 文件、1,279 项通过。新增测试按红绿顺序验证颜色、显式成员、稳定投影尺寸、冻结组拖动、颜色响应保留人工 Head、导航迟到隔离、历史栈身份、空卡删除同步分组、远处内容的检查点取景。

Bridge 使用真实临时目录和 HTTP，不以 UI mock 代替存储验证。覆盖原子失败、生命周期只读、分组 CAS、缺失成员冲突、50 批回执、六批各 100 张删除恢复、组复制新身份、BoardArtifact 映射、Checkpoint 副本及完整备份恢复。

最终门禁（23:04，本分支集成代码）：

| 命令 | 结果 |
| --- | --- |
| `pnpm test` | 121 文件、1,332 项通过 |
| `pnpm exec tsc --noEmit` | exit 0 |
| `pnpm build` | exit 0，保留独立 canvas-vendor 与所有既有 lazy feature chunks，无 500 kB 警告 |
| `pnpm build:bridge` | exit 0 |
| `git diff --check` | exit 0 |

Node v26.7.0 的全量测试出现基线已有的 localStorage ExperimentalWarning；不将该环境警告表述为无警告。没有新增依赖。

## 浏览器

所有操作只写隔离 workspace `/private/tmp/mira-grouping-qa.MHHen0`，独立 Node Host 58430、Vite 5179。已实际操作并核对服务端持久数据：

- 桌面 1440x900 两卡追加选择、批量设色、命名建组。
- 组选择不产生模型来源；标题整体拖动只移动成员，组外 Card 不动；撤销恢复整体位置。
- 一次把单卡拖到原框外，归属不变，框自动扩展。
- 明确移出后成员数量从 2 变 1，撤销恢复为 2。
- 复制整组后新增组和独立 Card，保留颜色，零 Transformation。
- 390x844 色板、按钮尺寸与页面溢出检查；Esc 关闭菜单并将焦点返回颜色入口。
- 浅/深色画布和色板目视检查；开发期严格模式 dialog 关闭事件、组框测量导致隐藏及拖动警告均已定位并补修。

最终生产构建复验：1440x900 浅/深色画布、实际组标题拖动、390x844 色板与分组菜单、Esc 焦点返回和检查点预览通过；该生产页面 console warning/error 为零。390px 页面 scrollWidth 为 390，色板宽 360、左右边界 15/375，全部按钮命中区 44x44。组框和颜色在刷新后保持，检查点预览显示两个命名框、四张卡的色条及组织差异计数。

本机截图证据在隔离 QA 目录：`desktop-dark.png`、`desktop-light.png`、`mobile-light-palette.png`、`mobile-checkpoint.png`。这些是当次验收截图，不是生产数据或发布产物。没有进行原生 Electron 窗口或重新打包验收，本批不修改 desktop 宿主或打包逻辑。

## 独立审查

实现 Agent 对集成 frontend 做只读审查，复现并交接了历史并发栈顶误删、颜色响应覆盖新分组、拖动成员未冻结、空卡删除组状态遗漏，以及已删除成员导致历史永久阻塞。主 Agent 增加对应回归并修复，后端冲突修复由独立切片提交后集成。另补充远离原点的检查点预览回归。
