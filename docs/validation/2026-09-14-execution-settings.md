# 项目默认与指导目录：第二批验证

日期：2026-09-14（Asia/Shanghai）。工作树 `.worktrees/step-execution`，分支 `codex/step-execution`，基于第一批 `472e750`。单 Agent 顺序实施、最终整合审查；没有推送、合入 main 或替换安装版。需求和剩余批次见[整体需求与进度](../refactor/generation-controls-requirements.md)。

## 产品命题

项目默认只影响之后创建的普通步骤、批量步骤和直接计划。指导目录保存名称、完整文本、来源与不可变版本；停用只撤下新选择，旧步骤仍能运行。配置保存零 Run，不执行文本内代码。完整备份 V4 带入目录与默认，旧 V1–V3 兼容，单 Board 包不安装目录。

## 实现正确性

- `test/bridge/execution-settings.test.js` 使用临时真实文件系统覆盖缺文件零写入、默认持久化、版本追加/停用/恢复、同基线并发冲突、非法字段、坏摘要和原子替换失败。真实应用路由与步骤/计划创建、真实 Run 存储证明：指导更新并停用后，原文仍进入执行器请求和 Run 快照。
- `src/domain/executionSettings.test.ts` 验证明确文本导入保留 Markdown 与代码块原文、不执行代码；拒绝错误扩展名、空白、NUL 和超长文本；目录仅列当前可选版本，历史仍可精确解析。
- V4 导出/恢复在独立新项目往返；坏规则在恢复前拒绝，staging 设置被改写后整次回滚，目标不存在且临时根清理；V3 恢复没有设置文件。既有 V1/V2、Checkpoint、材料和组织恢复测试继续通过。
- 新增回归测试修复方法库存储漏校验输出规则摘要，以及旧布局内部设置文件可被正文绑定覆盖的问题；失败保存不替换原对象。

最终运行：`pnpm test` **163 文件 / 1663 项通过**；`pnpm exec tsc --noEmit`、`pnpm build`、`pnpm build:bridge`、`git diff --check` 通过。Node 26 测试仍出现既有 experimental localStorage 警告，没有测试失败。

## UX 可发现性与浏览器

在临时演示 workspace 的真实浏览器验证 1280×800、1024×768 和 390×844。系统设置进入能力管理；明确保存项目默认、新建指导、保存版本 2、停用和恢复可选均成功。内置规则只读，来源与版本有文字，成功提示在编辑/切换后清除。长规则与 footer 可滚动到达；1024/390 的 document scrollWidth 等于视口宽度。

离开未保存指导出现统一保护，继续编辑保留草稿。通过另一个真实 HTTP 写入制造 revision 冲突，保存后显示冲突、原草稿保留且可选中复制，旧操作不会自动重试。390px 最后一个可用按钮按 Tab 返回侧栏关闭按钮，焦点不落到背景画布。浅色/深色与焦点样式已目视检查；浏览器捕获日志无 warning/error。

文本导入的格式、内容和来源链路有自动验证；本次没有通过原生文件选择器导入文件，也未验收手机软键盘。没有将这两项记为完成。

## 桌面证据

在本机 macOS arm64 运行定向 bridge/desktop 测试 **7 文件 / 73 项通过**；最终 `pnpm desktop:make:arm64`、`pnpm desktop:smoke:packed:arm64` 和 `node scripts/packed-desktop-smoke.mjs --arch=arm64 --restore` 通过。普通 smoke 同时核对 PDF 原件和 V4 备份包含项目设置；恢复 smoke 保留旧 V2/Checkpoint 兼容覆盖，V4 精确恢复由上述真实文件集成测试验证。

临时 userData/workspace 启动未安装 `.app`，真实原生窗口无 Board 时仍能进入能力管理、修改并保存默认文本，显示成功且焦点回到默认选择器，窗口布局无可见遮挡。首次手工验收路径尚未创建，宿主按预期显示 ENOENT；创建临时目录后重启完成验收。原生进程出现既有 fs.Stats 弃用和输入法 IMK 日志，未影响保存。未重新验收目录选择器，没有 Windows/Intel 原生或签名、公证、发布证据。临时进程与浏览器在验收后关闭。

## 产物质量与边界

本批证明规则选择、冻结和管理正确，不声称模型必然遵守语义风格。模型测试使用确定性执行器，不调用真实 Kimi、不产生模型费用；真实材料/模型质量对照仍待完成。UI 沿用现有连续侧栏、原生输入与草稿保护，未增加未实现的工具选项。MCP、Python、工具权限和调用循环仍属于后续批次。
