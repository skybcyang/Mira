# 本地安装、恢复与模型补充验收

2026-09-13，Asia/Shanghai。用户授权替换本机应用，并补充当前可执行的模型质量与升级回退验证。单 Agent 顺序执行；没有修改产品行为、提交、推送或公开发布。此前报告中的“未替换应用”是当时事实，本页记录其后的安装。

## 本地应用

macOS 26.3.1 (a)，Apple Silicon arm64。从 HEAD `c1f82ed` 加当前未提交实现构建；旧、新版本号均为 `0.1.0-beta.2`，用实际 `app.asar` SHA-256 区分：

- 旧：`f60403e857ea051515a4773b299cd36691d4b7d68f84d117762149678b7938d5`
- 新：`e202fe2d59ed0709be46e4f9da60459bb1fe078935622bac1cfbfd883ec99f05`

先完成 arm64 make、正常及恢复 packed smoke，再暂存完整应用。当前窗口无正文草稿，持久 Run 共 2 个、活动 Run 为 0；正常退出旧应用后才备份和更换。旧 `.app` 保留为独立带时间戳副本，不覆盖既有备份；新版已安装至 `/Applications/Mira.app` 并正常启动。主可执行文件确认为 Mach-O arm64，已安装 asar 摘要与新包一致。

原生窗口实际显示原工作区内容，以及“材料”“方法与计划”新入口；键盘打开系统设置，实际显示 `Mira 0.1.0-beta.2 · macOS ARM64`，Escape 关闭并恢复焦点。窗口截图人工检查导航与设置面板可见、无溢出遮挡。含用户内容的截图仅保留在本机私有备份目录，不写入仓库证据。本次没有取得原生 renderer console 的完整记录，不宣称零 warning/error，也不将这些操作扩大为全部原生交互验收。

## 数据与回退边界

正常退出后保存 5 个受管数据文件及桌面路径/窗口状态；备份目录权限为 0700。应用替换及验收后，原受管数据逐文件 SHA-256 与安装前完全一致。没有在用户画板中创建、编辑、删除或运行测试内容。

在独立副本，由新版导出正式 MiraBackup 并恢复到新目录，包含 2 个 Board、2 个 Run、1 条灵感；本份数据没有 Workflow 或 Checkpoint。新版 packed app 打开恢复副本并返回就绪；另由新版 Node runtime 在副本新建验收卡，关闭并重启后确认同一 Head 和内容仍存在。PDF 文字/空白判断与 PNG 渲染由本次 packed smoke 覆盖。

旧版实际执行从升级前备份恢复到另一空目录，并在原生窗口显示恢复画板。旧版恢复出的 5 个文件与新版恢复结果逐字节摘要相同，随后正常退出。首次用新版 smoke 脚本等待旧版 `smoke-ready` 标记失败；检查旧 asar 确认它没有该标记或 `MIRA_DESKTOP_SMOKE_EXIT_ON_READY`，因此不记为自动 smoke 通过，使用原生窗口与数据摘要作为回退证据。

本次证明“保留旧应用、将升级前备份恢复到新目录”的可用路径；**没有证明旧版可直接读写所有新版新增字段**，也没有把恢复覆盖到原工作区。用户引用的原文件正文不属于 MiraBackup，必须另行保管。签名、公证、正式 Release、Windows 与真实 Intel Mac 的升级/回退仍未验收。

私有安装回执、原始数据摘要、升级前备份和截图保留在本机 `Mira-backups/local-upgrade-qlUb2p`；旧应用路径由该目录内 `installation-receipt.json` 记录。回退前遵循[发行规程](../operations/desktop-release.md)，不直接覆盖已产生新工作的 workspace。

## 三次真实 Kimi 补充运行

使用已授权 Kimi 连接，实际模型 `k3`；在独立临时 workspace 执行普通 Run，共新增 3 次模型调用。未提交用户的真实工作区内容。完整冻结输入、输出、Run 与验证结果见[本轮证据](evidence/2026-09-13-followup-model-acceptance.json)。

| 材料与明确任务 | 实际结果 | 依据复核 |
| --- | --- | --- |
| 项目产品定义全文，19,175 字符；每个一级章节提取一个条目 | 11 章 → 11 项，输出 5,532 字符 | 逐章标题覆盖，无漏章或子标题重复拆项；关键否定、用户确认、未上线与不自动执行边界保留。第 5/8/9 章仍很长，不代表低审阅成本。 |
| 明确标注的三角色演示访谈，198 字符 | 首次 8 项 | 覆盖离线已保存摘录、在线生成等待、不要求自动找材料、原句后判断、数量不凑数、显式保存、不静默写、费用未决。说话者与原时间段对应，无新增共识或费用方案。 |
| 同一访谈、同一步骤显式重跑 | 再次 8 项 | 主张及否定/未决语义保持，但 itemId 和表述不同，不能据 ID 自动认定跨运行对应。演示字幕不代表真实访谈或视频画面理解。 |

首轮明确拆出 8 张卡，给其中一张加入验收人工备注。重跑只追加原清单版本，画板总卡数仍为 12，旧拆分卡及人工备注保持不变。依据实际输出明确选择同一“离线已保存摘录”观点，预览内容保留备注后逐卡采用：

- 旧 Head 提交返回 `CARD_VERSION_CONFLICT`。
- 正确基线提交仅给选中旧卡追加一个 human Version，备注保留。
- 其他卡片不变，没有新 Run 或自动拆分、删除。

以上是实施 Agent 对三个有限样本的依据核对，不是独立用户研究。历史字数超限及 PDF 补充解释失败未被本轮覆盖，仍按[前轮质量复核](2026-09-13-model-quality-review.md)保留；真人修改成本未测。累计真实调用为 23 次，B5 质量集仍未全绿。

## 工程验证与收尾

- 桌面定向：9 files / 90 tests passed。
- `pnpm test`：155 files / 1609 tests passed；保留既有 Node localStorage ExperimentalWarning。
- `pnpm exec tsc --noEmit`、`pnpm build`、`pnpm build:bridge`、`git diff --check`：通过。
- 本次 arm64 make、正常及恢复 packed smoke：通过；没有把此前 x64/Rosetta 结果记作本次重新执行。
- 三个真实模型 Run 均终态 succeeded，隔离模型服务已关闭；已安装 Mira 留给用户使用。

A2 尚需完整目录选择、文件交互、系统提示等原生任务验收；A3 真实触屏、A4 签名和其他原生目标、B5 稳定质量与真人修改成本仍待完成。
